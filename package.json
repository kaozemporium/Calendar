{
  "name": "kaoz-emporium-calendar-feed-generator",
  "description": "Standalone script that generates public/calendar.ics from Firestore, run on a schedule by GitHub Actions.",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
